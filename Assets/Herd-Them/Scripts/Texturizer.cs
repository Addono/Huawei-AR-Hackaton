using UnityEngine;

namespace Scripts
{
    public class Texturizer : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }
    }
}
